<!--
TEMPLATE: Run Context (Phase 0). Copy to specs/<run>/context.md at kickoff. Short — it frames the run.
Delete this comment in the real file.
-->
---
artifact: context
phase: 0
gate: "—"
status: set
updated: <YYYY-MM-DD>
---

# Run Context — <run>

- **Mode:** <new project (greenfield) | feature or change in an existing codebase>
- **Time budget:** <ample → standard path | tight → fast path (see `workflow/fast-path.md`)>
- **Rigor profile:** <prototype | standard | production> (see `profiles/`)
- **Overrides:** <e.g. observability → production; or none>

**Problem (one line):** <the problem this run solves>
